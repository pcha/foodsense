<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only. The Firebase emulators speak plain HTTP, and 10.0.2.2 is the address of the host
  machine as seen from an Android emulator. Scoped to that host so nothing else gains cleartext,
  and absent entirely from release builds.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
